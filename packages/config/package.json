{
  "name": "@projectmodern/config",
  "version": "2.0.0",
  "private": true,
  "description": "Shared configuration files for Project Modern",
  "files": ["tsconfig.base.json", "tsconfig.node.json", "tsconfig.react.json", "vitest.shared.ts"],
  "scripts": {
    "clean": "rm -rf node_modules"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
